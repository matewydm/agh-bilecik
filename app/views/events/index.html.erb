<h1> Wszystkie wydarzenia</h1>
<ul>
  <% for event in @events %>
    <li><%= link_to event.artist, "/events/#{event.id}" %></li>
  <% end %>
</ul>

<td><%= link_to 'Nowe ogłoszenie', events_new_path, class: 'btn btn-info btn-xs' %></td>
